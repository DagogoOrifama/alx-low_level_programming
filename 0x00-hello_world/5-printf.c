#include <stdio.h>

/**
 * main - Entry point                          *
 * Description: 'A program that prints a string using printf'
 *
 * Return: Always 0 (Success)                  */

int(void)
{
print("with proper grammar, but the outcome is a piece of art,\n")
return (0);
}
